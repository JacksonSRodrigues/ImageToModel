{
  "name": "image-segregator",
  "version": "0.1.0",
  "private": true,
  "main": "lib/main.js",
  "dependencies": {
    "get-pixels": "^3.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.2",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.24.1",
    "electron": "^1.6.10"
  },
  "scripts": {
    "start": "npm run build && electron -r babel-register .",
    "build": "babel src -d lib"
  }
}
